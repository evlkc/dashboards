import streamlit as st

st.set_page_config(page_title="Finance — Scanner", layout="wide")
st.title("📡 Finance — Scanner")
st.info("TODO: Port scanner logic into this page (remove old import app pattern).")
